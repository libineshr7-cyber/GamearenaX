/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.html"],
    theme: {
        extend: {
            fontFamily: {
                sans: ['Outfit', 'sans-serif'],
                rajdhani: ['Rajdhani', 'sans-serif']
            }
        }
    },
    plugins: [],
}
